apiVersion: v1
kind: Service
metadata:
  name: express-calculator-service
spec:
  selector:
    app: express-calculator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort  # Use LoadBalancer type if running on cloud providers like AWS, GCP, or Azure
